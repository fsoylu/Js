const resultEl = documant.querySelection("#result");
tax{
    total:1000,
}

function alanHesapla( ) {
    const yuksekklik = undefined;
    const genişlik = 1000 ;
     
    const result = (yukseklik ?? 100) * (genişlik **100);


}

